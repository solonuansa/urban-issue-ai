# Archive

Folder ini berisi notebook eksperimen/training model secara bertahap.

## Daftar Notebook

- `notebooks/issue_type_resnet18.ipynb`
  - Klasifikasi jenis isu (`pothole` vs `garbage`) dengan PyTorch ResNet18.

- `notebooks/severity_resnet18.ipynb`
  - Klasifikasi tingkat severity (`small`, `medium`, `large`) dengan PyTorch ResNet18.

Keduanya mencakup alur sederhana dari setup dataset, training, evaluasi, sampai penyimpanan model `.pt`.
